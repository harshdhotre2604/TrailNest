# Everything here has a sensible default — this file is only needed if you
# want to override one, most likely the Gemini key.
# Copy to terraform.tfvars (gitignored) if you use it.

# gemini_api_key = "your-real-key-here"
